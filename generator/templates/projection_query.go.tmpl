package projection

import (
	"context"

	"gorm.io/gorm"
)

// Query functions are added here by `esb add query`.
// Each function reads from the projection database (read model).

// Example: GetByID returns a row by its aggregate ID.
// func GetByID(ctx context.Context, db *gorm.DB, id string) (*SomeRow, error) {
// 	var row SomeRow
// 	err := db.WithContext(ctx).Where("aggregate_id = ?", id).First(&row).Error
// 	return &row, err
// }

var _ = (*gorm.DB)(nil) // keep import
var _ = context.Background
